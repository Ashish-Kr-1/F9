# ─── Cloud Run Environment Variables Template ───
# Set these in Cloud Run → Edit & Deploy → Variables

# Database — Cloud SQL (PostgreSQL 15)
DATABASE_URL=postgresql://xp_admin:Test%401234@/xpwebos?host=/cloudsql/YOUR_PROJECT:YOUR_REGION:YOUR_INSTANCE

# Connection pool limits (Cloud Run scales instances, keep pool small)
DB_POOL_MAX=5

# Google Cloud Storage bucket
GCS_BUCKET_NAME=xp-web-os-storage

# JWT secret (generate a strong random string)
JWT_SECRET=CHANGE_ME_TO_A_STRONG_SECRET

# Port (Cloud Run sets this automatically, but you can override)
PORT=8080
